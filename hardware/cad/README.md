# CAD files

Placeholder for this project's physical CAD files — reactor mount, sensor bracket, LED strip enclosure, level-shifter/Pi housing, or anything else specific to this build's physical assembly.

Add source files here (e.g. `.f3d`/Fusion 360, `.step`/`.stp`, `.dwg`, `.skp`, or whatever tool was used to design them), plus an exported `.stl`/`.step` per part if it's meant to be 3D-printed or shared with someone using different CAD software. A short note next to each file on what it is and which BOM component (see [`../BOM.md`](../BOM.md)) it corresponds to is enough — no fixed naming convention required yet.
