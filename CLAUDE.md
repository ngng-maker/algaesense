# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository. Keep it current — see "Development log" at the bottom for the standing instruction to log mistakes and confirmed-good patterns here as they happen, not just at milestones.

## What this project is

AlgaeSense is an agentic monitoring/control system for an IoT algae (*Arthrospira*/*Spirulina platensis*) cultivation bioreactor: VOC emissions via an Alphasense PID sensor, biomass via a Pi camera (greenness-based), symbolic regression + active-learning experiment design via the upstream `jaxsr` package, and a conversational agent (Hermes Agent + Slack) that can fit models, suggest next experiments, and adjust the LED — with a human confirmation gate before anything touches hardware.

It started from two spec documents under `Resources/` (`jaxsr_augmentation_spec.md`, the authoritative software spec; `spirulina_voc_experimentalist_protocol.md`, the human lab protocol) describing a `jaxsr-calibration` package. The build went well beyond that original spec's scope — see Architecture below for what actually exists now.

## Repository status

All three planned packages are built, tested, and (for `algaesense-edge`) wired to specific, confirmed real hardware. This is a `uv` workspace monorepo; each package is also independently `pip install`-able.

```
algaesense/
├── CLAUDE.md
├── Resources/                      # original spec docs (background/history, not authoritative for current code)
├── docs/
│   ├── hardware_setup.md           # SSH setup + Pi/LED power sequencing for THIS project's confirmed wiring
│   └── slack_and_hermes_setup.md   # Hermes install, Anthropic key, Slack app creation walkthrough
├── packages/
│   ├── jaxsr-calibration/          # hardware-agnostic "brain": calibration, diagnostics, processing, camera
│   ├── algaesense-edge/            # Raspberry Pi hardware I/O: acquisition, actuators, network API
│   └── algaesense-agent/           # MCP servers Hermes connects to: mcp_pipeline, mcp_actuators, dashboard, labwiki, profile
└── pyproject.toml                  # uv workspace root (members = ["packages/*"], not itself installable)
```

## Architecture

Three packages, each with its own `pyproject.toml`, `src/` (src-layout), and `tests/{unit,integration,fixtures}/`:

- **`jaxsr-calibration`** — never touches hardware. Calibration (standard-addition, reference-jar cross-check, camera zero-point), diagnostics (fleet-zero, ambient baseline, swap-pilot, weekly audit), processing (covariate regression, common-mode subtraction, spectral filtering, dual-rate VOC/camera fusion), raw schemas (`logging_/schema.py`: `VOC_RAW_SCHEMA`, `CAMERA_RAW_SCHEMA`, `ExperimentMeta`). Depends on and calls the real upstream `jaxsr` package directly (`jaxsr.SymbolicRegressor`, `jaxsr.ActiveLearner`, `jaxsr.BasisLibrary`, etc.) — `jaxsr_calibration` is its own top-level import root, not merged into `jaxsr.*`'s namespace (upstream has no plugin mechanism for that). CLI: `jaxsr-cal`.
- **`algaesense-edge`** — runs ON the Raspberry Pi. Sensor acquisition (VOC PID via ADS1115 ADC, optional T/RH via BME280, camera via picamera2), actuator control (LED via WS2811 addressable strip, a `ControlProfileActuator` Protocol so a future heater/cooler can plug into the same control-profile engine — see `actuators/actuators.py`), a small FastAPI network API so the brain machine never needs SSH into the Pi for data. Re-validates every actuator setpoint against configured safety bounds independently of whatever the caller sends (`UnsafeSetpointError`) — this is the actual safety boundary, not a suggestion. `AppState.control_actuators`/`active_control_profiles`/`last_applied_setpoint` are keyed by `(reactor_id, actuator_kind)`, generic across actuator kinds even though LED is the only one implemented; `AppState.led_actuators` stays LED-specific for the manual single-setpoint path. `AcquisitionService.tick_control_profiles` drives whichever actuators have an active profile, once per tick, and caches each successfully-applied value into `last_applied_setpoint` — `run_voc_tick` reads that cache to record the LED's actual PAR into each VOC row (`reactor_par_umol_m2_s`). CLI: `algaesense-edge`.
- **`algaesense-agent`** — runs on the "brain" server, never on the Pi. Six MCP servers Hermes Agent (a real, separately-installed conversational agent) connects to as a client over stdio: `mcp_pipeline` (read-only: fit/suggest via jaxsr, plus `discover_led_response_dynamics` — feeds one experiment's real VOC time-series, with the LED's actually-applied PAR as a second state variable, into `jaxsr.discover_dynamics` for SINDy-style ODE discovery; meant for control-profile runs, where PAR genuinely varies within the run, unlike a static setpoint), `mcp_actuators` (propose/apply split — `apply_led_change`, `apply_led_profile_change`, and `stop_led_profile_change` are the only tools with a side effect; a "profile" is a declarative dict, `{"shape": "ramp"|"sinusoid"|"step"|"constant", ...that shape's own params}` — the agent only ever supplies typed parameters, never code, and `algaesense_edge.actuators.control_profiles.evaluate_control_profile` plus the target actuator's own bounds-check are what actually run it, every tick, on the edge service), `dashboard` (on-demand plot MCP tool + a separate live Streamlit app), `labwiki` (Karpathy LLM-wiki-pattern knowledge base: raw sources, index/log/summaries/entities pages, ingest/query/lint), `mcp_calibration` (guided step-by-step wizards for standard-addition/reference-jar/camera-zero calibration — durable file-backed session state per in-progress calibration, real math from `jaxsr_calibration.calibration`/`camera.calibration`, no hardware touched so no propose/apply gate needed), `mcp_diagnostics` (read-only: fleet-zero/ambient-baseline/swap-pilot/weekly-audit over already-collected raw VOC Parquet files, loaded via `raw_readers.load_raw_voc_readings` — a module-level shared loader, since `mcp_pipeline`'s dynamics-discovery tool needs the identical raw-loading capability). `profile/` holds the Hermes system prompt and config templates — not Python code Hermes imports.

Data layout (unchanged from the original spec):
- Raw: `data/raw/experiments/{experiment_id}/{sensor_id|camera_id}={value}/hour=YYYY-MM-DDTHH.parquet`
- Derived features: `data/derived/features/{campaign_id}/{experiment_id}.parquet`
- Labwiki: `data/labwiki/{campaign_id}/{raw/,wiki/}`

## Setup & running

Each package installs independently:
```
pip install -e packages/jaxsr-calibration
pip install -e "packages/algaesense-edge[hardware]"   # [hardware] only installs/imports on the Pi
pip install -e "packages/algaesense-agent[dev]"
```

Tests, **run per package separately** (not combined in one `pytest` invocation — each package defines its own top-level `tests/__init__.py`, and pytest's import system collides across them if both paths are given at once):
```
pytest packages/jaxsr-calibration/tests
pytest packages/algaesense-edge/tests
pytest packages/algaesense-agent/tests
```

`algaesense-edge` and `algaesense-agent` both use `@pytest.mark.hardware` for tests that need real hardware (WS2811 strip, ADS1115, camera) — skipped by default (`addopts = "-m 'not hardware'"`), run for real on the Pi with `pytest -m hardware`.

For the Streamlit live dashboard: `streamlit run packages/algaesense-agent/src/algaesense_agent/dashboard/streamlit_app.py` (a `.claude/launch.json` config named `streamlit-dashboard` exists for `preview_start`).

For Hermes/Slack/Anthropic setup and Pi hardware/SSH setup: see `docs/slack_and_hermes_setup.md` and `docs/hardware_setup.md`.

## Confirmed real hardware (algaesense-edge)

- **Board**: Raspberry Pi 4 (confirmed 2026-07-17) — matters specifically for the LED below.
- **VOC sensor**: Alphasense PID + ISB (analog 0-3.3V output) → **ADS1115 ADC** (Adafruit STEMMA QT), I2C address **0x48**, channel 0 (A0), single-ended.
- **T/RH sensor**: not yet acquired. `Bme280TRHSensorReader` is a real, ready driver for when one is added — until then, `trh_reader` is `None` throughout and `sample_t_c`/`sample_rh_pct` are recorded as null (schema already supports this).
- **LED**: WS2811 addressable RGB strip (ALITOVE), **GPIO18** (BCM), through a 74AHCT125 level shifter (3.3V→5V) + 470ohm resistor, powered from a separate 12V supply with common ground to the Pi. Driven via `adafruit-circuitpython-neopixel`, **pixel_order="BRG"** (confirmed by testing — not the library's GRB default). `num_pixels` has no default; it must match the real strip's actual count. `rpi_ws281x` (underneath the neopixel library) has solid, long-standing support on Pi 4's GPIO/PWM architecture — this would be a real, current concern on a Pi 5 instead (RP1 chip moved GPIO handling, older PWM+DMA libraries don't uniformly support it yet), but doesn't apply here.
- **Camera**: Raspberry Pi Camera Module v1 (OV5647), CSI connection, via `picamera2`.

## Pi-side OS/permission requirements (not just `pip install`) -- see `docs/hardware_setup.md` for the full walkthrough

- **GPIO/PWM access needs root** — `rpi_ws281x` needs direct DMA/PWM hardware access; run `algaesense-edge start`/`scan-i2c` with `sudo`, using the venv's Python explicitly (`sudo .venv/bin/algaesense-edge ...`) since `sudo` resets `PATH`.
- **Onboard audio conflicts with the LED's PWM channel** — both use the same PWM peripheral. Set `dtparam=audio=off` in `/boot/config.txt` (or `/boot/firmware/config.txt` on newer releases) and reboot before testing the LED.
- **`picamera2` needs `sudo apt install -y python3-picamera2`**, not just the `[hardware]` pip extra — it depends on system `libcamera` bindings pip alone won't provide. If the venv can't see it, recreate the venv with `--system-site-packages`.
- **Camera format mismatch — confirmed real, fixed 2026-07-21**: `picamera2`'s `H264Encoder` always writes a raw H.264 elementary stream regardless of the output path's extension; `run_camera_tick` names every clip `.mp4`, so `process_clip`'s `cv2.VideoCapture` reliably failed to open them (`moov atom not found` — not an OpenCV-build quirk, the file genuinely wasn't a valid MP4). Fixed: `Picamera2CameraCapture.record_clip` now records through picamera2's `FfmpegOutput`, muxing a real MP4 directly — `ffmpeg` is now a required system dependency (`sudo apt install -y ffmpeg`), not just an occasional manual-remux tool.

## Coding conventions

- **Comments**: a short, simple docstring for `help()`/introspection (what it does, no technical detail), plus a *separate* triple-quoted string block placed above the corresponding code for developer-facing rationale/non-obvious detail — not `#` comments, and not folded into the docstring. Blank-line spacing between chunks for readability. This is a deliberate, explicit project convention (not the tool's usual "minimal comments" default).
  - **Exception: Streamlit entry-point scripts** (anything run via `streamlit run x.py`, e.g. `dashboard/streamlit_app.py`). Streamlit's "magic commands" feature renders every bare top-level string expression as literal page content — confirmed by testing — including ones inside a function body once called; only the true first-statement docstring is exempt. Use plain `#` comments in those files instead. This does NOT apply to modules merely imported by a Streamlit app, only to the script actually passed to `streamlit run`.
- **No markdown tables in chat responses to this user** — use lists/headers (horizontal scroll on tables is inconvenient for them). This is about chat responses, not file content — tables inside project markdown docs are fine.
- **Plain-English explanation before code** when explaining how a system/codebase works, especially for "explain X to me" style requests — lead with a narrative story (no file names, no code), then go to code/file-level detail only once asked or for "show me the code" style requests.
- Ousterhout "A Philosophy of Software Design" principles apply throughout: deep modules over shallow ones, avoid needless duplication across subpackages, separate general-purpose from special-purpose code, pull complexity downward, define errors out of existence where reasonable, don't merge genuinely distinct ideas into one file/class just to reduce file count.
- Mock/fake hardware or service stand-ins are **not used** in this codebase as of 2026-07-16 (see Development log) — real hardware classes and real in-process apps (e.g. `httpx.ASGITransport` against the actual FastAPI app) are used instead, even in tests. Pure numeric/text test fixtures for hardware-independent logic (e.g. a synthetic video file to test `process_clip`, synthetic Parquet rows to test a fit) are fine and are not "mocks" in the sense that was ruled out — the distinction is stand-in *hardware/service* implementations vs. plain *input data* for pure functions.

## External tools & accounts this project depends on

- **`jaxsr`** — real, installed upstream package (github.com/jkitchin/jaxsr) providing `SymbolicRegressor`, `ActiveLearner`, `BasisLibrary`, `Constraints`, etc. `jaxsr_calibration` depends on and calls it directly.
- **`mcp`** (official Model Context Protocol Python SDK) — used to build the four `algaesense-agent` MCP servers (`mcp.server.fastmcp.FastMCP`).
- **Hermes Agent** (`pip install hermes-agent`, Nous Research) — real, pip-installable, MIT-licensed. Acts as an **MCP client** (connects to our servers via `~/.hermes/config.yaml`, stdio subprocesses) plus owns its own Slack gateway and cron scheduler. We do not write code that runs inside Hermes; we write servers it connects to.
- **Anthropic API key** — needed for Hermes's own LLM calls (`hermes config set ANTHROPIC_API_KEY ...`, never pasted into a chat session).
- **Slack app + bot token** — see `docs/slack_and_hermes_setup.md` for creating it (manifest generator: `hermes slack manifest --agent-view --write`).
- **Streamlit**, **slack_sdk**, **pandas** — the live dashboard's dependencies (in `algaesense-agent`'s main deps, not an extra, since the dashboard ships as part of the package).

## Known gotchas (found the hard way — don't reintroduce)

- **"PAR" values are an approximation, not a calibrated measurement** — `LEDActuator.par_per_full_duty_umol_m2_s` is meant to come from a real quantum/PAR meter, but absent one, it's derived from a phone lux-meter reading times a rough, LED-spectrum-dependent lux-to-PPFD conversion factor (see `docs/hardware_setup.md` §7). An earlier session's docstring incorrectly attributed this constant to the experimentalist protocol's lux-meter step, which is actually just a one-time photoinhibition safety ceiling check (≤15,000 lux), not a PAR calibration — the two are unrelated measurements that happen to both use a lux meter. Every PAR value anywhere in this system (live setpoints, logged control-profile YAML, any future dynamics-discovery output) inherits this approximation's uncertainty until real PAR-meter equipment replaces it — don't treat a discovered light-response equation's coefficients as physically exact.
- **pyarrow Hive-partitioning false positive**: `pyarrow.parquet.read_table(path)` auto-detects Hive-style partitioning from ANY `key=value` segment in a file's path — including this project's own raw-data layout (`sensor_id=PID01/hour=...parquet`) — and invents a phantom partition column that collides with the real same-named data column already in the file (`ArrowTypeError: Unable to merge: Field sensor_id has incompatible types: string vs dictionary<...>`). Fix: read via `polars.read_parquet(path)` instead (no such auto-detection), `.to_arrow().cast(schema)` if you need it back as a pyarrow Table for `concat_tables`. Affects `algaesense_edge/acquisition/writer.py`; watch for it anywhere else that reads files back from this partition layout with raw pyarrow.
- **Streamlit magic commands** — see Coding conventions above.
- **This repo has no git history before 2026-07-16** — it was a plain directory until a safety checkpoint commit was made right before a large, hard-to-reverse deletion pass (removing all mock/fake test doubles). If you're about to do something similarly sweeping and destructive, check `git status`/commit a checkpoint first, same as then.
- **Cross-package `pytest` collision** — see Setup & running above; always run each package's tests as a separate invocation.
- **`reactor_par_umol_m2_s` is `None` for every VOC row logged before this field started being populated** (recorded from `AppState.last_applied_setpoint`, wired up 2026-07-18) — `discover_led_response_dynamics` raises a clear `ValueError` if the whole column is null for a requested selection rather than feeding NaN into `jaxsr`, but if you're debugging "why does this old experiment have no PAR history," this is why: the row exists, the column just predates the fix. Also a one-tick (~1s) lag: `run_voc_tick` runs before `tick_control_profiles` in `cli.py`'s loop, so a VOC row's cached PAR reflects the *previous* tick's applied value, not the one about to be set this tick — negligible against the PID sensor's own physical response time, but worth knowing if a light/VOC correlation looks off by one sample.

## Standing/deferred requirements (not yet built — confirm scope before starting)

- **Physical hardware verification** — the hardware-backed code (`voc.py`, `camera.py`, `actuators.py`, etc.) is written against confirmed real specs but has not been run on the actual Pi yet (this dev environment is a remote laptop, not the Pi, and has no network path to it even when the user is on a Tailscale-connected second laptop — remote-controlling one machine from another doesn't route the controlled machine's traffic through the controller's VPN). Run `pytest -m hardware` on the Pi itself once physically accessible, per `docs/hardware_setup.md` — which also covers several known OS-level risks (root/sudo for GPIO, onboard-audio PWM conflict, `picamera2`'s apt dependency, a likely H.264/mp4 camera format mismatch) that are real and specific to this hardware (confirmed Raspberry Pi 4), not yet verified against it, and not fixable from this dev machine alone. User has deliberately deferred this until the rest of the software is complete, to batch-test everything at once rather than push to GitHub piecemeal.

## Development log

**Standing instruction**: whenever something fails and gets fixed, an approach is confirmed correct after genuine uncertainty, or a non-obvious lesson surfaces during work on this repo, add a dated entry below. Keep entries short: what happened, why, what to do differently (or keep doing) as a result. This is a shared engineering log for this repo, not a personal memory — write it so any future session (human or Claude) can use it without re-deriving the lesson.

### 2026-07-16

- **Removed all mock/fake hardware and service test-doubles project-wide** (`MockLEDHardware`, `MockVOCSensorReader`, `MockTRHSensorReader`, `MockCameraCapture`, `GpiozeroLEDHardware`, `httpx.MockTransport` fake-handler tests, a `FakeSlackChannel` test harness) at the user's explicit request, after they confirmed understanding it would reduce automated test coverage on non-Pi dev machines. Replaced with: real hardware classes (tests split via `@pytest.mark.hardware`), real in-process FastAPI apps via `httpx.ASGITransport` instead of hand-written fake HTTP handlers, and removing the fake-Slack simulation entirely (the propose/apply tool split is what's actually verified, not a simulated chat). **Lesson**: this kind of instruction has large blast radius across the whole codebase — always init/checkpoint git first if no version control safety net exists yet, and read every affected file's real, current content before rewriting (don't rely on memory of what was there).
- **`pip install streamlit` silently downgraded `pyarrow` (25.0.0 → 24.0.0)** in the shared venv, which then surfaced the Hive-partitioning bug above as a test failure. **Lesson**: after installing a new dependency into a shared environment, always re-run the full test suite for every package, not just the one being worked on — a transitive dependency conflict can break unrelated code silently.
- **Confirmed real Hermes Agent architecture via direct web research before writing any MCP server code**, rather than assuming from training knowledge: it's a real pip package (`hermes-agent`), acts as an MCP *client* (not a place to run server code), and has its own Slack gateway + cron scheduler configured via `~/.hermes/config.yaml` and its own CLI. **Lesson**: for any library/framework claim central to an architecture decision, verify via WebSearch/WebFetch against current docs rather than relying on training-data recall, especially when the claim is old (from an earlier planning session) and might have changed or been imprecise.
- **`neopixel.NeoPixel`'s `pixel_order` accepts a plain string directly** (e.g. `"BRG"`) — it is not required to be one of the library's predefined name constants (`RGB`/`GRB`/`BGR`/`RGBW`/`GRBW`), which don't include `BRG` at all. Caught before shipping by checking the library's real source rather than assuming a `getattr(neopixel, "BRG")` lookup would work.

### 2026-07-17

- **Built the guided calibration wizard** (`algaesense-agent`'s new `mcp_calibration` server): step-by-step standard-addition/reference-jar/camera-zero flows, each backed by a durable YAML session file (`mcp_calibration/sessions.py`) rather than in-memory state, since a session can span many minutes across separate tool calls and an in-memory dict would be lost if the MCP server subprocess restarted mid-session. All the actual math/persistence reuses existing, already-tested `jaxsr_calibration` functions (`fit_sensitivity_per_sensor`, `compute_fleet_ratios`, `compute_blank_baseline` + their real `persist_*` functions) — the wizard's own code is only sequencing and data collection.
- **A tool function's `from x import Y` placed *inside* the function body can't be monkeypatched via `monkeypatch.setattr(module, "Y", ...)`** — the patch target has to be a module-level name for that to work, since a function-local import re-resolves `Y` fresh from its own source module on every call, ignoring whatever the test patched onto the *calling* module's namespace. Hit this writing `record_camera_zero_step_from_edge`'s test; fixed by moving the `EdgeClient` import to module level and introducing an overridable `_build_edge_client()` factory function (mirroring the pattern `mcp_actuators/server.py` already used for exactly this reason) as the actual patch target. **Lesson**: any MCP tool that constructs a network client internally should build it via a small local factory function, not inline in the tool body, purely so tests can inject a fake transport through it later.
- **`EdgeClient` only had `recent_voc_readings`, no `recent_camera_readings`**, even though `algaesense-edge`'s API has exposed `GET /sensors/camera/recent` since Phase 1b — the gap only became visible when the camera-zero calibration wizard needed to pull a live camera reading. **Lesson**: when a new feature needs to call an existing service, double-check the client wrapper actually has a method for every endpoint the service exposes, don't assume symmetry with a sibling method (`recent_voc_readings`) that happens to already exist.
- **Confirmed the user's Pi is a Raspberry Pi 4** (not a Pi 5) — removes the `rpi_ws281x`/RP1-chip compatibility concern for the LED strip. Also confirmed this dev laptop has no network path to the Pi even when the user is simultaneously on a Tailscale-connected second laptop and remote-controlling this one from it — remote desktop/screen sharing doesn't route the controlled machine's network traffic through the controller's VPN. **Lesson**: don't assume a network path exists just because the user has one from their own vantage point; verify directly (a plain `ping`) before planning around it.
- **Built `mcp_diagnostics`** (fleet-zero/ambient-baseline/swap-pilot/weekly-audit as MCP tools). One real gotcha: `AmbientBaselineResult.covariate_models` holds `CovariateModel` objects whose `covariance` field is a raw numpy array and whose `symbolic_regressor` field can be a live, unpicklable `jaxsr.SymbolicRegressor` — neither survives a generic `dataclasses.asdict()` + JSON round-trip the way every other diagnostic result here does. **Lesson**: before wrapping any dataclass-returning function as an MCP tool, check every field's actual type for numpy arrays or live library objects, not just assume `asdict()` is JSON-safe because it worked for the last three tools.

### 2026-07-18

- **Built agent-generated LED control profiles end-to-end**, resolving the design fork noted above: user explicitly picked the declarative-parameters option over arbitrary generated code. Shape: `algaesense_edge.actuators.control_profiles.evaluate_control_profile` (pure function, four shapes: constant/ramp/sinusoid/step) is the only code that ever runs a profile; `AppState.active_led_profiles` tracks one `ActiveLedProfile` (profile dict + start time) per reactor; `AcquisitionService.tick_led_profiles` evaluates and applies every reactor's active profile once per acquisition tick (~1 Hz, same cadence as VOC sampling) via the existing `LEDActuator.set_par()` — meaning the bounds-check re-runs continuously for the life of a running profile, not just once at start. A rejected value stops the profile and turns the LED off rather than continuing with a partially-applied schedule. New edge API endpoints (`POST`/`DELETE /actuators/led/{reactor_id}/profile`) start/stop a profile and best-effort log a YAML record of what was started under `data/raw/experiments/{experiment_id}/control_profiles/`; new `mcp_actuators` tools (`propose_led_profile_change`/`apply_led_profile_change`/`stop_led_profile_change`) expose this to Hermes, with `stop_*` deliberately exempted from the propose-first confirmation gate (stopping an already-approved action needs no fresh review).
- **Starting/stopping a profile never touches real hardware — only a running profile's per-tick evaluation does.** This mattered for test design: every test for the new `POST`/`DELETE /actuators/led/{reactor_id}/profile` endpoints and the new `mcp_actuators` tools runs with no `@pytest.mark.hardware` mark at all (they only exercise `AppState`'s in-memory validate-and-record logic), while `AcquisitionService.tick_led_profiles`'s own tests need the mark for both its "applied" branch (calls real `LEDActuator.set_par` → `hardware.set_duty_cycle`) and its "rejected" branch (calls `turn_off()`, which also reaches real hardware) — only the "no active profile" and "no registered actuator" branches are hardware-free. **Lesson**: when a new method has multiple branches, check each one individually for whether it reaches a real hardware/service call before deciding which tests need the `hardware` mark — don't assume the whole method is one hardware/non-hardware unit. (`tick_led_profiles`/`active_led_profiles`/`start_led_profile`/`stop_led_profile` named in this entry were renamed to their generic `*_control_profiles`/`ActiveControlProfile` equivalents later the same day — see the entry below.)
- **Generalized the control-profile engine to be actuator-kind-agnostic, and fixed two real inaccuracies, before building the JAXSR dynamics-discovery integration.** User asked how JAXSR's real capabilities connect to control profiles; while scoping that (a parked sub-plan — see `C:\Users\JenLab\.claude\plans\jazzy-kindling-hickey.md` if it still exists at time of reading), two things surfaced: (1) control profiles were 100% LED-hardcoded even though `TemperatureActuator`/`StirringActuator` already exist as unimplemented stubs — fixed by adding a `ControlProfileActuator` Protocol (`apply_setpoint`/`turn_off`) and re-keying `AppState`'s tracking dicts to `(reactor_id, actuator_kind)`, while deliberately leaving the LED-specific manual-setpoint path and profile API routes/MCP tool names untouched (no functional payoff to renaming those until a second actuator kind exists); (2) `LEDActuator`'s docstring incorrectly attributed `par_per_full_duty_umol_m2_s` to the experimentalist protocol's lux-meter step, which is actually just a one-time photoinhibition safety ceiling check (≤15,000 lux), not a PAR calibration — checking the actual source document (`Resources/spirulina_voc_experimentalist_protocol.md:124`) rather than trusting an earlier session's claim caught this. **Lesson**: (a) when a user's question exposes that two features (control profiles, future actuators) should share an abstraction, generalize the shared engine before building the next feature on top of the narrower version — cheaper than a rewrite later; (b) a docstring's claim about *why* a design decision was made is itself a claim to verify against the actual source, not just trust from an earlier session, especially when it's about to matter for a new feature (PAR values feeding into a "real" scientific ODE-discovery result).
- **Built `discover_led_response_dynamics`** (new `mcp_pipeline` tool): feeds one experiment's real per-second VOC trajectory, with the LED's actually-applied PAR as a second state variable, into `jaxsr.discover_dynamics`. The entire underlying math chain (`apply_calibration` → `fuse_multirate` → `load_timeseries_for_jaxsr` → `jaxsr.discover_dynamics`) already existed and was already proven correct by `jaxsr-calibration`'s own `test_milestone4_single_experiment_trajectory_feeds_real_jaxsr_dynamics_discovery` integration test — the only real gap was that nothing recorded what PAR was actually applied at each VOC sample (`reactor_par_umol_m2_s` was hardcoded `None` in `service.py`). Fixed via `AppState.last_applied_setpoint` (a generic cache, not LED-specific, updated wherever `apply_setpoint`/`set_par` succeeds) which `run_voc_tick` now reads. `apply_calibration`'s `data_dir` argument needed to be the exact directory holding `{calibration_run_id}.parquet`/`.yaml` (`{data_dir}/derived/calibrations/standard_addition`), not the top-level data dir — easy to get wrong since `load_raw_voc_readings`'s `data_dir` argument IS the top-level dir; confirmed the exact subpath by checking `mcp_calibration/server.py`'s existing `persist_calibration` call rather than guessing. **Lesson**: when two different functions both take a `data_dir`-shaped parameter, don't assume they mean the same directory — check what each one actually does with it (here: one appends `raw/experiments/{id}/...` itself, the other expects the caller to have already appended the right subpath).
- **Relocated `load_raw_voc_readings`/`NoRawReadingsFoundError` from `mcp_diagnostics/diagnostics.py` into a new shared `algaesense_agent/raw_readers.py`** once `mcp_pipeline`'s new dynamics-discovery tool needed the identical raw-Parquet-loading capability — de-duplicating a second real consumer, not a speculative generalization. Initially re-exported the two names back through `mcp_diagnostics/diagnostics.py` for "compatibility," then caught that this is exactly the kind of backwards-compatibility shim this project's conventions explicitly disfavor, and fixed it by updating the actual call sites (`mcp_diagnostics/__init__.py`'s re-export, `test_mcp_diagnostics.py`'s import) to source the two names from the new module directly instead. **Lesson**: catch yourself reaching for a "keep the old import working" shim and stop — this project's own stated convention is to update the real call sites cleanly, not paper over a move.

### 2026-07-19

- **Real bugs surfaced by a multi-agent skill-testing exercise, fixed in `jaxsr-calibration`.** Built a general-purpose "fan-out/debate/fan-in" multi-agent skill family (`package-plan`/`package-develop`/`package-test`, see `~/.claude/skills/`) and ran a lightweight test of `package-plan` using `jaxsr-calibration` itself as the planning objective. The 5 planning agents ignored an instruction not to read the real repo, turning the exercise into a genuine grounded code review that surfaced concrete, real production issues — since these were well-specified and low-risk, they were fixed directly rather than left as findings: (1) `run_ambient_baseline`'s `method` guard lived inside its per-sensor loop, so a zero-sensor `readings` frame silently swallowed an invalid method instead of rejecting it — moved the guard before the loop; (2) `persist_calibration` and `fleet_zero._write_result` wrote Parquet/YAML directly to their final paths with no atomicity, so a crash mid-write could corrupt calibration/diagnostic output — switched both to write-to-temp-then-`Path.replace()`; (3) seven functions (`fit_covariate_model`, `apply_covariate_correction`, `run_ambient_baseline`, `run_fleet_zero`, `run_swap_pilot`, `compute_fleet_ratios`, `fit_sensitivity_per_sensor`) dereferenced required DataFrame columns with no upfront check, surfacing opaque polars errors instead of caller-legible ones — added a shared `jaxsr_calibration/validation.py` (`require_columns`/`require_implemented_method`) and applied it everywhere; (4) shipped a real `robust` method (RLM/Theil-Sen) alongside `ols` in `fit_covariate_model`/`fit_sensitivity_per_sensor`, which exposed a second-order bug in the process — `apply_covariate_correction`'s gate checked `model.method != "ols"` exactly, so a fitted `robust` model would have silently never actually been applied; fixed to `not in ("ols", "robust")`; (5) two of `tests/fixtures/synthetic_readings.py`'s six generators (`make_swap_pilot_readings`, `make_standard_addition_readings`) built rows via Python loops instead of vectorized numpy like the other four — rewrote both, verified bit-for-bit identical output against captured pre-rewrite reference values under a fixed seed before trusting the rewrite. **Lesson**: (a) a subagent given filesystem tools will use them even when explicitly told not to, if the answer is discoverable — don't design a "blind" test that assumes an instruction alone will stop that; (b) grounding a multi-perspective debate in real code turns it from abstract architecture debate into an actual code review, which can be more valuable than the exercise it was originally meant to be, but changes what you're actually testing; (c) when a bug-hunting process finds something genuinely fixable in a few lines with a clear test, fix it immediately rather than filing it — the write-up-then-triage overhead often costs more than the fix itself.

### 2026-07-20

- **Ran the `package-test` skill (3-agent fan-out/debate/fan-in) against the real, already-built `algaesense-edge`/`algaesense-agent` code**, then fixed everything the synthesized plan flagged except one deliberately-declined item. Six real bugs fixed, in priority order: (1) `AcquisitionService.tick_control_profiles` (`service.py`) called `evaluate_control_profile(...)` *outside* its own `try/except UnsafeSetpointError` block — a schema-valid-at-start-but-malformed-at-evaluation profile (e.g. a step segment missing `par_umol_m2_s`, which `validate_control_profile` never checked either) raised an uncaught exception straight out of the tick loop, and `cli.py`'s acquisition loop has no try/except around this call at all — meaning that single bad profile would silently kill VOC + camera acquisition together while the FastAPI server and `/health` stayed green throughout. Fixed by widening the try block to wrap evaluation too (`except Exception`, treated identically to a safety rejection: turn off, cache 0.0, stop the profile) and by adding the missing `par_umol_m2_s` check to `validate_control_profile`'s step-segment loop in `control_profiles.py`. (2) `PartitionedParquetWriter.flush()` (`algaesense-edge/acquisition/writer.py`) and `mcp_calibration/sessions.py`'s `_write()` both wrote directly to their final path with no atomicity, same class of bug as 2026-07-19's `persist_calibration`/`fleet_zero._write_result` fix — both switched to write-to-temp-then-`Path.replace()`. (3) `discover_led_response_dynamics` (`mcp_pipeline/pipeline.py`) filtered a tz-aware UTC `timestamp` column against a possibly-naive `since`/`until` with no normalization (ambiguous — could silently filter the wrong window) — now raises a clear `ValueError` for a naive `since`/`until` rather than guessing the caller meant UTC; also, its all-null `reactor_par_umol_m2_s` guard missed the equally-real *partially*-null case (PAR recording starting mid-experiment, e.g. after a service restart) — added a second check with a distinguishing error message. (4) `EdgeClient.set_led`/`start_led_profile` (`mcp_actuators/edge_client.py`) treated every 422 `detail` the same way, but FastAPI's own pydantic request-validation errors shape `detail` as a LIST while this project's own domain-level rejections (`SetpointRejectedError`/`ProfileRejectedError`) always use a plain string — conflating the two misreported a bug in the request this client built as if it were the edge service's safety validation correctly doing its job. Fixed via a new `EdgeRequestShapeError` and a shared `_raise_for_domain_rejection` helper that dispatches on `isinstance(detail, str)`. (5) `AppState.recent_voc_readings`/`recent_camera_readings` (`api/state.py`) used `readings[-limit:]` unconditionally — `limit=0` should mean "return none," but `readings[-0:]` is Python for `readings[0:]`, the WHOLE list, not an empty one. Fixed by special-casing `limit <= 0`. (6) `LEDActuator.set_par`'s exact-maximum boundary case (`test_led.py`) only had `@pytest.mark.hardware` coverage even though the boundary comparison itself is pure arithmetic that runs before any hardware call — added a minimal inert `_InertLEDHardware` test double (bare `LEDHardware` Protocol conformance, no simulated behavior beyond "remember the last value set") so this one comparison runs on every test invocation, not just on the Pi; kept the original hardware-marked test too, renamed, for real-device confirmation. One item from the synthesis was deliberately NOT built: a test-only `ControlProfileActuator` fake to close a gap in `tick_control_profiles`'s rejection-path test coverage — 2 of 3 debate agents flagged this as reopening the user's own explicit, deliberate "no mock/fake hardware" decision from 2026-07-16, so it was surfaced to the user directly rather than resolved by agent consensus; user said no, leave it hardware-gated. All three package suites pass after every fix (160 + 53 + 62 = 275, no regressions). **Lesson**: (a) an uncaught exception on a background acquisition thread with no top-level try/except is a recurring real bug shape in this codebase (see also 2026-07-18's PAR-caching work) — any new per-tick code path needs to be checked for whether ALL of it, not just the "expected" failure mode, sits inside error handling that can't escape the thread; (b) a fan-out/debate/fan-in review run against real, finished code (not a plan) surfaces a different, often more concrete class of bug than planning-stage review — worth running once a feature is "done," not only before it's built; (c) when a multi-agent debate proposes working around a standing, user-confirmed project convention (however narrowly scoped the proposed exception), that decision goes back to the user explicitly — it is not something agent consensus alone gets to re-open.

### 2026-07-21

- **Published this repo to GitHub and did the first real Pi bring-up from it** (`git clone` onto both the Pi and a Tailscale-connected laptop acting as the "brain" machine), surfacing three confirmed real install-time gotchas not caught by any dev-machine testing here, since none of them can occur without a fresh clone on real, unconfigured Debian-based machines: (1) `algaesense-agent[dev]`'s `dev` extra lists `algaesense-edge` as a test-only dependency (so its own test suite can spin up a real edge FastAPI app in-process) — installing `algaesense-agent[dev]` on a machine outside this repo's `uv` workspace fails with `No matching distribution found for algaesense-edge`, since that package was never published to PyPI. Fix: don't install the `[dev]` extra to actually *run* the system, only to run its own test suite — `pip install -e packages/algaesense-agent` (no extra) is the correct command for the brain machine, and the README's install instructions were updated to match. (2) Modern Debian-based Raspberry Pi OS enforces PEP 668 (`error: externally-managed-environment`) — `pip install` refuses to touch system Python at all now, not just as a "you probably shouldn't" warning. Fix is the standard one already documented here (`python3 -m venv .venv && source .venv/bin/activate` first), just newly confirmed as a hard blocker rather than a soft one on current Pi OS. (3) pip's own `picamera2` package (as opposed to the apt-packaged `python3-picamera2`) pulls in `python-prctl`, which needs `libcap`'s development headers to compile and fails outright without them (`You need to install libcap development headers to build this module`) — a real, previously-undocumented blocker on top of the already-known "picamera2 needs the apt package for real libcamera bindings" gotcha. Fixed the doc: `sudo apt install -y python3-picamera2 libcap-dev` before recreating the venv with `--system-site-packages`, so pip sees the apt-installed `picamera2` as already satisfied instead of trying to rebuild it (and its `python-prctl` dependency) from source at all. **Lesson**: dev-machine testing (however thorough) cannot surface OS-packaging-policy gotchas (PEP 668) or transitive-build-dependency gotchas (`python-prctl`/`libcap-dev`) that only exist on the *target* machine's actual OS/Python version — these three were only found by actually running the real install commands on the real Pi and laptop, confirming `docs/hardware_setup.md`'s own repeated caveat ("written against confirmed specs, not yet verified against real hardware") was exactly the right level of hedging to have kept in there.
- **A fourth, more serious gotcha from the same bring-up: `.gitignore`'s `data/` rule was silently excluding a real shipped source file, not just locally-generated output.** Running `pytest packages/jaxsr-calibration/tests` on both the freshly-cloned Pi and brain laptop failed 12 tests with `FileNotFoundError` from `CalibrationGas.builtin()` — the built-in VOC response-factor table it loads via `importlib.resources.files(...) / "data" / "response_factors.yaml"` simply didn't exist on either machine. Root cause: `.gitignore`'s `data/` entry (meant only for the repo-root's locally-generated experiment output — raw Parquet, calibration outputs) was unanchored, so it ALSO matched `packages/jaxsr-calibration/src/jaxsr_calibration/calibration/data/`, a real package resource directory — meaning `response_factors.yaml` was never actually committed to git at all, despite being present and working on the original dev machine (which still had the file sitting locally, uncommitted, from before this repo's very first commit). Every fresh clone anywhere else was missing it. Fixed by anchoring the rule to `/data/` (repo root only) and committing the previously-invisible file; confirmed 34/34 previously-failing tests pass after the fix. **Lesson**: an unanchored `.gitignore` pattern matching a common directory name (`data/`, `build/`, `output/`, etc.) can silently swallow a real, needed file anywhere else in the repo tree with the same name — and because the file still exists locally on whichever machine created it, this class of bug is invisible until someone does a genuinely fresh `git clone` and runs the tests there, not caught by any amount of testing on the original machine. Worth a quick `git ls-files | grep <suspicious-dirname>` sanity check against anything nested under a directory sharing a name with a root-level `.gitignore` entry.
- **A fifth gotcha from the same Pi bring-up, this one a real test-design gap rather than a packaging bug: 5 of algaesense-edge's "fails clearly without the hardware extra installed" tests (`test_led.py`, `test_voc.py` x2, `test_camera.py`, `test_i2c.py`) permanently fail on the Pi itself** (`RuntimeError: NeoPixel support requires running with sudo`, `ValueError: No I2C device at address: 0x76`, `Failed: DID NOT RAISE ImportError`, etc.), because their whole premise — the hardware library genuinely isn't installed — is false on the one machine where the `[hardware]` extra is (correctly) installed for real use. These aren't regressions; they're just meaningless on the Pi. Fixed by adding `packages/algaesense-edge/tests/conftest.py`'s `hardware_extra_importable(*module_names)` helper and having each of those 5 tests `pytest.skip(...)` when its precondition (the library genuinely absent) doesn't hold, rather than asserting something false. Confirmed unchanged behavior on a non-Pi dev machine (53 passed, same as before) and expect these 5 to now skip cleanly on the Pi instead of permanently failing. **Lesson**: a test whose docstring says "X isn't installed on this dev machine" is implicitly assuming something about the environment it runs in — once a project has a machine (like this one's Pi) where that assumption is deliberately false, the test needs to check its own precondition and skip rather than silently becoming a permanent, uninformative failure on that machine.
- **A sixth, purely operational gotcha from the same bring-up: an incorrect Tailscale IP produced a long, misleading troubleshooting detour that looked like an SSH problem, then a firewall problem, then a service-not-running problem — when the real issue was simply the wrong address the whole time.** The user had confirmed (at the time, via the Tailscale admin console) that a specific IP was the Pi's; it wasn't actually the Pi. Every symptom that followed was explainable purely by that mismatch: a `ping` to the wrong address returned suspiciously instantly (sub-1ms) with `TTL=128`, which is Windows' default TTL, not Linux's default of `64` (Raspberry Pi OS) — a real, catchable tell that went unheeded for a while; SSH and the network API both got "connection refused" against that address, which looked like a Pi-side problem (dead `sshd`, firewall, crashed service) even though the Pi itself was completely healthy and reachable the entire time at its *actual* address. The real fix was just running `tailscale status` (cross-checked against a self-referential rule for the Pi's real IP that was already visible, unnoticed, in an earlier `iptables -L` output) to get the ground-truth address, at which point every previously-"broken" thing — SSH, the health endpoint, live sensor reads, and remote LED control — worked immediately with zero code changes. Documented as its own subsection in `docs/hardware_setup.md` §2 ("Connecting over Tailscale specifically: verify the IP first, don't assume it"). **Lesson**: when troubleshooting a remote-machine connectivity issue produces a growing pile of seemingly-unrelated symptoms (SSH refused, firewall looks fine, service looks fine, port refused) that don't individually resolve anything, stop and re-verify the address/identity of the remote machine itself before continuing to dig into each individual symptom — a wrong-target problem masquerades as N different broken-target problems, and no amount of debugging the target fixes a mistaken address.
- **First real `pytest -m hardware` run on the actual Pi, three genuine findings.** (1) The BME280 test (`test_bme280_reader_reads_real_temperature_and_humidity_from_hardware`) fails with `ValueError: No I2C device at address: 0x76` — entirely expected, not a bug: no T/RH sensor is physically wired up yet (see "Confirmed real hardware" above), and the test's own docstring says as much. (2) `pytest -m hardware` itself needs `sudo`, same as `algaesense-edge start`/`scan-i2c` — easy to forget since running tests doesn't feel like a hardware-touching action. Without root, a first LED test fails cleanly (`RuntimeError: NeoPixel support requires running with sudo`), but a *second* LED test in the same process can then hit a hard `Segmentation fault` instead of another clean failure — the first failed attempt leaves `rpi_ws281x`'s underlying C-level module state partially initialized, and the next attempt crashes the whole interpreter rather than raising. Documented in `docs/hardware_setup.md`'s pixel-count section. (3) The long-flagged "likely" camera format mismatch (see the gotcha above, now resolved) turned out to be real and reproducible every time, not an edge case — fixed via `FfmpegOutput`. That fix in turn surfaced a real duration floor: `test_phase1b_end_to_end.py`'s abbreviated capture window (originally 0.5s, "to keep the test fast") doesn't give `FfmpegOutput` enough real frame data to mux a valid MP4 -- there's genuine startup overhead eating into the requested duration before frames actually land (a direct 2s/5fps hardware check yielded only 2 real frames, not the naively-expected 10). Bumped in two steps (0.5s -> 1.0s, still failing -> 2.0s, confirmed clean) rather than guessed once. **Lesson**: (a) a test failing for a reason unrelated to the code under test (missing physical hardware) is a signal to check the test's own stated assumptions before treating it as a regression; (b) a segfault on real hardware is worth reading carefully rather than dismissed as "flaky" — this one had a clear, traceable root cause (missing root privileges corrupting shared C-level state) once the actual command being run was checked against what the hardware genuinely requires.
- **A real production bug, not just a test artifact: `Picamera2CameraCapture.record_clip` never called `camera.close()`.** Surfaced once the duration-floor fix above let `test_phase1b_end_to_end.py`'s camera step succeed for the first time -- the very next camera-touching test in the same process (`test_picamera2_records_a_real_clip_from_hardware`) then failed with `RuntimeError: Failed to acquire camera: Device or resource busy` / `Camera in Configured state trying acquire() requiring state Available`, even though the isolated single-file run of that same test had passed cleanly earlier. Root cause: `Picamera2.__init__` registers every instance with a global camera-manager singleton that keeps a live reference, holding the device in `Configured` state until explicitly closed -- ordinary Python garbage collection of the local `camera` variable never reaches it, since the manager singleton is the thing actually holding the reference open. This is not merely a test-isolation quirk: **the real, long-running `algaesense-edge start` service calls `record_clip` fresh on every hourly camera tick from the same process**, so every capture after the very first one in any real, multi-hour experiment run would have silently failed the exact same way. Fixed by adding `camera.close()` alongside the existing `camera.stop_recording()` in the `finally` block. **Lesson**: a hardware library's own object lifecycle (not just Python's normal refcounting/GC) can hold a resource open indefinitely if the library registers instances with an internal singleton/manager -- always check a hardware SDK's own documented cleanup call (`.close()`, `.release()`, etc.) rather than assuming garbage collection alone frees the underlying device, especially for anything that gets constructed repeatedly inside a long-running loop.
